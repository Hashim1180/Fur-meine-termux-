{
  "version": 2,
  "env": {
    "APP_ID": "@app-id",
    "APP_SECRET": "@app-secret",
    "DATABASE_URL": "@database-url"
  },
  "buildCommand": "cd app && npm ci && npm run vercel-build",
  "outputDirectory": "app/dist"
}
